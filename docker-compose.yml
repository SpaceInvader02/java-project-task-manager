services:
  task-service:
    build: .
    ports:
      - "${SERVER_PORT:-8080}:8080"
    environment:
      SERVER_PORT: 8080
      TASK_WORKERS: ${TASK_WORKERS:-4}
      TASK_PROCESSING_DURATION: ${TASK_PROCESSING_DURATION:-2s}
